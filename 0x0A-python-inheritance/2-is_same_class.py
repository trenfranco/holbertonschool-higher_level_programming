#!/usr/bin/python3
"""function that checks id  object is exactly
    an instance of the specified class"""


def is_same_class(obj, a_class):
    """returns true or false"""
    return (type(obj) == a_class)
